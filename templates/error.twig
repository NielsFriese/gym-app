{% extends "layout.twig" %}

{% block content %}
    <div class="alert alert-danger">
        <h3>Fehler</h3>
        <p>{{ error }}</p>
    </div>

    <a href="{{ url_for('home') }}" class="btn btn-primary">Zurück zur Startseite</a>
{% endblock %}